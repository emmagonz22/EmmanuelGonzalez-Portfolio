<nav class="navbar mobile-nav is-black">
 
    <div class="navbrand" id="navback">
      <a role="button" class="navbar-burger" class="table-content-navbar" aria-label="menu" aria-expanded="false" id="open-table" href="#">
        <i class="fa fa-bars" aria-hidden="true"></i>
      </a>
    </div>
  

  <div class="navbar-menu" id="navbarMenuWrapper">
    <div class="navbar-start" id="navbarMenu">
      <a class="navbar-item item" href="#about">About</a>
      <a class="navbar-item item" href="#education">Education</a>
      <a class="navbar-item item" href="#skills">Skills</a>
      <a class="navbar-item item" href="#worke">Work Experience</a>
      <a class="navbar-item item" href="#workr">Research Experience</a>
      <a class="navbar-item item" href="#projects">Projects</a>
      <a class="navbar-item item" href="#achi">Achievements</a>
    </div>
  </div>
</nav>

<script>
  const tableContentButton = document.getElementById("open-table");
  const navbarMenuWrapper = document.getElementById("navbarMenuWrapper");
  const navbarBackground = document.getElementById("navback");
  const iconElement = tableContentButton?.querySelector('i'); // Target the icon inside the button

  if(navbarBackground){
    navbarBackground.setAttribute('data-theme', 'light');
	  tableContentButton?.addEventListener('click', () => {
      if (navbarBackground.getAttribute('data-theme') === 'light') {
          navbarBackground.setAttribute('data-theme', 'dark');
          navbarBackground.setAttribute('style', 'background-color: rgba(0, 0, 0, .2); box-shadow: none;');
          iconElement?.classList.remove('fa-bars');
          iconElement?.classList.add('fa-xmark');
          
      } else {
          navbarBackground.setAttribute('data-theme', 'light');
          navbarBackground.setAttribute('style', 'background-color: white; box-shadow: rgba(60, 64, 67, 0.3) 0px 1px 2px 0px, #36658826 0px 2px 6px 2px;');
          iconElement?.classList.remove('fa-xmark');
          iconElement?.classList.add('fa-bars');
          console.log(navbarBackground)
      }
    });
  }
  

  if (tableContentButton) {
    tableContentButton.addEventListener("click", (e) => {
      e.preventDefault(); // Prevent the default anchor behavior
      if (navbarMenuWrapper) {
        navbarMenuWrapper.classList.toggle("is-active");

      }
    });
  }
</script>

  
<style> 


.mobile-nav {
  display: flex;
  width: 100%;
  height: 40px;
  flex-direction: column;
  justify-content: center;
  justify-self: center;
  position: absolute;
  top: 0;
  left: 0;
  }


.navbrand {
  display: flex;
  position: fixed;
  top: 15px;
  right: 40px;
  /*
  left: 50%;
  transform: translateX(-50%);
  width: 85%;
  */
  width: 40px;
  height: 40px;
  align-items: center;
  border-radius: 24px;
  color: white;
  box-shadow: rgba(60, 64, 67, 0.3) 0px 1px 2px 0px, #36658826 0px 2px 6px 2px;
  z-index: 999 !important;
  background-color: white;
  outline: none;
}

.menu-title {
  height: 100%;
  display: flex;
  margin-left: 15px;
  margin-top: -1px;
  align-items: end;

}
.text-gradient {
    font-family: "Ubuntu", sans-serif;
		background-image: var(--primary-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}

.table-content-navbar {
  display: flex;
  position: fixed;
  top: 10px;
  right: 40px;
  height: 40px !important;
  width: 40px !important;
  justify-content: center;
  align-items: center;
  border-radius: 100px;
  z-index: 1000 !important;
}

.navbar-burger{
  border-radius: 100px !important;
}

.navbar-menu {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  opacity: 0;
  visibility: hidden;
  transform: translateX(-100%); 
  transition: transform 0.4s ease, opacity 0.4s ease, visibility .4s ease;
  background-color: transparent;
}

/* Keyframe animation for sliding in */
@keyframes slideInFromLeft {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

@keyframes slideOutToLeft {
  from {
    transform: translateX(100%);
    opacity: 1;
  }
  to {
    transform: translateX(0);
    opacity: 0;
  }
}

.navbar.is-black{
  background-color: transparent;
}
.navbar-menu.is-active {
  animation: slideInFromLeft 0.8s ease forwards;
  display: flex; 
  opacity: 1;  
  visibility: visible;  
  background-color: rgba(0, 0, 0, .93); 
  z-index: 995;
}

.navbar-menu:not(.is-active) {
  animation: slideOutToLeft 0.8s ease forwards;
}
.navbar-start {
  flex-direction: column;
  text-align: center;
  height: 100%; 
  align-content: space-evenly;
}

.navbar-item {
  color: white;
  font-size: 20px;
  margin: 10px;
  text-decoration: none;
  font-family: "Ubuntu", sans-serif;
}

.navbar-item:hover {
  color: rgb(225, 225, 225);
}


@media (min-width: 768px) {
  .mobile-nav {
    display: none !important;
  }

}

/* Optional: Transition effect for smooth menu opening */
.navbar-menu {
  transition: all 0.3s ease;
  opacity: 0;
  visibility: hidden;
}

.navbar-menu.is-active {
  opacity: 1;
  visibility: visible;
}

    
</style>